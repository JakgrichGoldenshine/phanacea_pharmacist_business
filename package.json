{
  "name": "phanacea-pharmacist-business",
  "version": "1.0.0",
  "private": true,
  "description": "PHANACEA Pharmacist Business — run the whole product (backend API + frontend) via `node server.js`",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
